eureka:
  instance:
    hostname: budgetbuddy-ri8k.onrender.com
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: https://${eureka.instance.hostname}:${server.port}/eureka/
server:
  port: 8761
